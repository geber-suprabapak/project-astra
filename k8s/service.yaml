apiVersion: v1
kind: Service
metadata:
  name: project-astra
  labels:
    app.kubernetes.io/name: project-astra
spec:
  selector:
    app.kubernetes.io/name: project-astra
  ports:
    - name: http
      port: 80
      targetPort: 3000
